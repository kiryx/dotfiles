- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.local/bin: bin/
    ~/.bashrc: bashrc
    ~/.xmodmaprc: xmodmaprc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
